# Error Log

- **[2026-03-03] powershell `npx serve` blocked:** Execution of `npx serve` was blocked by Windows PowerShell execution policies. Workaround: Ran inside WSL using `wsl -e bash -c "npx serve -l 8080 ."`.
- **[2026-03-03] Hardcoded OpenRouter URL in proxy logic:** Found that `/api/chat.js` proxy and `script.js` local fetch paths were still hardcoded to OpenRouter API even if another provider (e.g. Qwen, Mistral) was selected. Fixed by extracting `endpoint` resolution into dynamic mapped `if-else` blocks based on `SESSION.provider`.
- **[2026-03-03] Browser subagent DOM focus tracking:** During browser verification of provider API key placeholders, UI dropdown elements were sometimes mis-targeted due to overlapping `z-index` and complex native `<select>` lists. Workaround: validated via direct DOM properties rather than simulated coordinate clicks.
- **[2026-03-04] Grok SVG visually rendering as a cross:** The exact SVG provided in the agent instructions (`<line x1="18" y1="4" x2="6" y2="20">...`) drew a cross ('X') rather than a single slash ('/'). Verified in browser layout, but deployed exactly as instructed on `script.js`.
- **[2026-03-04] Agent 12 Audit — Legacy `vercel_script.js` stores keys in localStorage:** This 503-line legacy file uses `localStorage.setItem(STORAGE_KEY, openRouterKey)` (line 126) for API key storage. Not loaded by production `app.html`, but should be deleted along with `vercel_app.html` and `vercel_style.css` before final deployment to eliminate risk.
- **[2026-03-04] Agent 12 Audit — No bugs found in production code:** Full security scan, asset verification, and browser testing completed. Zero hardcoded keys, correct provider SVGs, all UI features functional.
- **[2026-03-04] Agent 12 DEEP AUDIT — `applySeatModel()` hardcoded provider map:** Only 5 of 17 providers were mapped (openai→ChatGPT, anthropic→Claude, google→Gemini, x-ai→Grok, meta→Llama). Swapping to DeepSeek/Mistral/Qwen showed raw provider prefix as name. Also `meta` check never matched `meta-llama/` prefix. FIXED: Added `PROVIDER_DISPLAY_NAMES` map.
- **[2026-03-04] Agent 12 DEEP AUDIT — `updateAvatarVisuals()` dead code + wrong fallback:** Had 4 no-op normalization lines (`if (x === 'x') x = 'x'`). Fallback SVG used `<i class="fa-solid fa-robot">` but Font Awesome isn't loaded. Same dead code in `renderSeats()`. FIXED: Cleaned up, unified fallback to inline SVG circle.
- **[2026-03-05] Victor's LLM4Ultra Merge — Vercel Proxy Bypassed:** In the merged codebase, `isHosted` was hardcoded to `false`, bypassing the secure proxy. Fixed by setting `const IS_HOSTED = window.location.hostname !== 'localhost' && window.location.hostname !== '127.0.0.1';` and ensuring api calls route to `/api/chat` correctly.
- **[2026-03-05] Victor's LLM4Ultra Merge — Demo Mode Lockout:** The original `fetchAIResponse` required `SESSION.apiKey` even when `isHosted` was true, which would block Vercel-side keys. Fixed by adding `!IS_HOSTED` to the demo mode guard.
- **[2026-03-05] Grok 3 Migration:** Upgraded Grok configuration from `grok-beta` to `grok-3` and `grok-3-mini` across all seat models and provider mappings.
- **[2026-03-05] Legacy File Risk:** Confirmed `vercel_script.js`, `vercel_app.html`, and `vercel_style.css` are persistent security risks due to old `localStorage` key handling. Scheduled for deletion.
- **[2026-03-10] Vercel API Rate Limit Adjustment:** Increased `MAX_REQUESTS_PER_HOUR` in `api/chat.js` from 20 to 200, and added a developer toggle `ENABLE_RATE_LIMIT = true` to allow bypassing IP limits entirely.
- **[2026-03-10] Grok API Timeout Analysis:** Confirmed Grok timeouts (`[Grok is unavailable — skipping]`) are isolated provider-side rate limits (503s from x.AI). The system grace-skips correctly without crashing the frontend.
